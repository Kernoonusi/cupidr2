import { SwipeSkeleton } from "@/components/swipes/swipeSkeleton";

export default function Loading() {
  return (
    <main className="max-w-2xl mx-auto px-4 relative">
      <SwipeSkeleton/>
    </main>
  );
}
